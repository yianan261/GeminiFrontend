import 'package:flutter/material.dart';

class UploadGoogleTakeoutPage extends StatelessWidget {
  const UploadGoogleTakeoutPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      body: const Center(
        child: Text('Upload GoogleTakeout Page!'),
      ),
    );
  }
}
